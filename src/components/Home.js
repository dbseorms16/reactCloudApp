import React, { Component } from "react";
import Card from "@material-ui/core/Card";
import CardContent from "@material-ui/core/CardContent";

class Home extends Component {
  render() {
    return (
      <Card>
        <CardContent>리액트 파이어베이스 기반</CardContent>
      </Card>
    );
  }
}

export default Home;
